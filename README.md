# HandWrittenDigitRecognitionUsingTensorflow
he project demonstrates the complete Deep Learning workflow, including data preprocessing, normalization, neural network creation, model training, evaluation, and prediction. A Sequential Neural Network with Dense layers, ReLU activation, and Softmax output is used to classify the digits accurately.
